<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="CSS/styles.css">
    <title>To Do List</title>
</head>
<body>
    <div class="box" id="heading">
        <h1><%=kindOfDay%></h1>
    </div>

    <div class="box">
        <%for(var i=0;i<newListItems.length;i++){%>
        <form action="/delete" method="post">
            <div class="item">
                <input type="checkbox" onclick="this.form.submit()" value="<%=newListItems[i].id%>" name="checkbox">
                <p><%=newListItems[i].name%></p>
            </div>
            <input type="hidden" name="listName" value="<%=kindOfDay%>">
        </form>
        <%}%>
        <form class="item" action="/" method="post">
            <input type="text" name="newItems" placeholder="New Item" autocomplete="off">
            <button type="submit" name="button" value="<%=kindOfDay%>">+</button>
        </form>
    </div>

    
</body>
</html>